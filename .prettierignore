dist
dev-dist
coverage
package-lock.json
supabase/.temp
supabase/.branches

# not ours: the product spec, kept in the working tree but never committed
02-especificacion-tecnica.md

# Generated by `npm run db:types`; compared byte for byte in CI.
src/lib/database.types.ts

# The design-sync lib fork. Upstream formatting, kept verbatim so it can be
# diffed against the bundled lib/source-kit.mjs on every re-sync.
.design-sync/overrides
